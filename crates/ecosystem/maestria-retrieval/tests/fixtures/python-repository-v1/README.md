# Python Repository Intelligence fixture

A small, frozen Python repository used by the `python-repository-frozen-v1`
benchmark corpus: a PEP 621 manifest, a `src/wishlist` package with a class,
functions, imports, and a call chain, plus a `tests/` directory.
