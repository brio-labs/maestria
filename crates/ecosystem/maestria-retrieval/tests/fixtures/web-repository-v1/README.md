# web-repository-v1 fixture

A small TypeScript package (`ui-kit`) used by the frozen
`web-repository-frozen-v1` benchmark corpus. It has a `src/` tree with a
component, a class, an interface, a type alias, an import chain, and a
`tests/` directory.

# todo: this fixture comment must not become a code marker (it is not a
# source symbol, so the marker query abstains).
