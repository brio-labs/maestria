[package]
name = "maestria-domain"
edition = "2024"
license.workspace = true
version = "0.6.0"

[dependencies]
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
