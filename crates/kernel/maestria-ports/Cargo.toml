[package]
name = "maestria-ports"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
contract-tests = ["dep:maestria-test-support"]

[dependencies]
maestria_domain = { package = "maestria-domain", path = "../maestria-domain" }
serde = { version = "1", features = ["derive"] }
maestria-test-support = { path = "../../test-support/maestria-test-support", optional = true }



[dev-dependencies]
maestria-test-support = { path = "../../test-support/maestria-test-support" }
