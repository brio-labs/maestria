{
  "name": "maestria-studio-web",
  "version": "0.6.1",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": { "node": ">=24.0.0 <25" },
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.css -o ./assets/tailwind.css --minify",
    "build": "corepack pnpm@9.15.9 run build:css && dx build --release --debug-symbols false && rm -rf dist && dx bundle --release --debug-symbols false --platform web --out-dir dist && for asset in dist/public/*; do mv \"$asset\" dist/; done && rmdir dist/public && cp assets/tailwind.css dist/assets/tailwind.css && python3 scripts/prune-dioxus-assets.py dist",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.54.1",
    "@tailwindcss/cli": "4.3.3",
    "concurrently": "10.0.4",
    "tailwindcss": "4.3.3"
  }
}
